# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
orbs:
  sonarqube: clicklogiq/sonarqube@1.0.1
  
jobs:
  say-hello:
    steps:
      - run:
        name: "Setup custom environment variables"
        command: echo 'export organization="Rocas Inc."' >> "$BASH_ENV"
        command: echo 'export host_url="http://rocasinc.com:9000"' >> "$BASH_ENV"
        command: echo 'export login="squ_99b453521d8104875d4e478ec486a7e14a44daa4"' >> "$BASH_ENV"
        command: echo 'export projectKey="my:project"' >> "$BASH_ENV"
# Invoke jobs via workflows    
# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - sonarqube/analyze